<% content_for :title, "albums" %>

<div class="w-full px-5">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl mb-5">Seu álbum</h1>
  </div>
  
  <% if @albums.any? %>
    <% @albums.each do |album| %>
      <%= render album %>
    <% end %>
  <% else %>
    <%= link_to "New album", new_album_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
  <% end %>
</div>
